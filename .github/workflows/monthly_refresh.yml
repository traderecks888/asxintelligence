name: Monthly ASX refresh

on:
  workflow_dispatch: {}

permissions:
  contents: write
  issues: write

concurrency:
  group: monthly-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Validate screener JS (syntax)
        run: |
          node --version
          node --check public/screener.js

      - name: Run valuation refresh (monthly)
        run: |
          python asx_intrinsic_valuations_export.py --resume --inputs_mode public --public_universe_url "https://raw.githubusercontent.com/traderecks999/data/refs/heads/main/asx/universe_latest.json" --public_prices_url "https://raw.githubusercontent.com/traderecks999/data/refs/heads/main/asx/prices_latest.json" --public_fundamentals_url "https://raw.githubusercontent.com/traderecks999/data/refs/heads/main/asx/fundamentals_latest.json" --update_mode full --generate_rrg --rrg_tf all --public_dir public/data --sleep 0.15 --history_period 5y --benchmark ^AXJO

      - name: Smoke check outputs exist
        run: |
          test -f public/data/manifest.json
          test -f public/data/latest_web.json
          test -f public/data/latest.xlsx

      - name: Upload debug artifacts (only if failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: asxintelligence-monthly-debug
          retention-days: 14
          if-no-files-found: ignore
          path: |
            public/data/manifest.json
            public/data/latest_web.json
            public/data/latest.xlsx

      - name: Commit updated data (public/data)
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data || true
          if git diff --cached --quiet; then
            echo "No data changes to commit."
            exit 0
          fi
          git commit -m "data: monthly refresh public dataset"
          git push

      - name: Create GitHub issue on failure
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const title = `ASX monthly refresh failed (${new Date().toISOString().slice(0,10)})`;
            const body = [
              "The scheduled ASX monthly refresh workflow failed.",
              "",
              `Run: ${runUrl}`,
              "",
              "Tip: open the workflow logs; the smoke-check step usually pinpoints missing outputs.",
            ].join("\n");
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              body
            });
