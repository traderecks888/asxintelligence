# UI Tooltip Patch v0.52

Adds hover tooltips on the table header for:
- Score (Screener Score)
- Value / Quality / Risk (component scores)

Apply:
1) Unzip into repo root (overwrite), commit + push.
2) Hard refresh screener.html (Ctrl+F5 / Cmd+Shift+R) to bust cached JS.
