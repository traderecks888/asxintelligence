# Patch v0.76 – Fix JS syntax error: duplicate async keyword

Fix:
- Replaces `async async function load()` with `async function load()` in `public/screener.js`

Apply:
Unzip into repo root (overwrite), commit + push, re-run workflow.

Edits applied: 1 replacement(s).
